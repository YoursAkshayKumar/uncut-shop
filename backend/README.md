# Backend Environment

Create a `.env` file in `backend/` with your secrets. At minimum, Razorpay needs:

```
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
```

Restart the backend after adding or updating these values.

